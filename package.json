{
  "name": "functional-form-module",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js", 
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client",
    "build": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
